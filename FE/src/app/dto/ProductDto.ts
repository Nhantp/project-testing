export interface ProductDto {
  productId?: number;
  productName: string;
  sellingPrice: number;
  screenSize: string;
  fontCamera: string;
  backCamera: string;
  productCpu: string;
  imageUrl: string;
  productStorage: string;
  description: string;
  category: number;
  brand: number;
}
